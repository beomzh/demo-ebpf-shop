# 촬영 런북 — 배송 조회 실패 사건

영상별로 **촬영 직전에 무엇을 실행하고, 화면에서 무엇을 보여주고, 어떻게 말하는지**를 정리한 문서입니다.
준비·배포는 [README](../README.md)를 먼저 보세요.

- Observ 화면에서는 네임스페이스 필터를 **`shop`** 으로 둡니다. (`demo-infra` 의 부하 발생기·DNS 는 데모 장치입니다)
- 트랜잭션 조회의 소스 토글은 **eBPF** 로 둡니다.

---

## 0. 촬영 전날 — 정상 상태 데이터 쌓기

```bash
./demo.sh deploy          # 배포 직후 = 정상 상태
./demo.sh status          # DNS → 예전 IP, TCP 연결 성공, 배송 조회 HTTP 200
```

- 최소 몇 시간, 가능하면 하루 이상 그대로 둡니다. 데모 3에서 조회 기간을 넓혀 예전 IP 로 정상 연결되던 모습과 비교합니다.
- `./demo.sh traffic` 으로 `201`(주문 생성)과 `200`(배송 조회)이 계속 찍히는지 확인합니다.

## 1. 사건 발생 — 촬영 시작 15~30분 전

```bash
./demo.sh incident        # 택배사가 IP 변경 (DNS → 새 IP). 방화벽은 예전 IP 만 허용된 상태
./demo.sh status          # DNS → 새 IP, TCP 연결 실패, 배송 조회 HTTP 502 약 5초
```

- SLO 경고와 T-Map 의 5초 영역이 보일 만큼 데이터가 쌓이도록 **촬영 전에 충분히** 둡니다.
- `./demo.sh traffic` 에서 배송 조회가 `502 5.0xs` 로 찍히면 정상적으로 장애 상태입니다.

---

## 영상 ① 발견 (10분) — 데모 1

| 순서 | 화면 | 보여줄 것 | 말할 것 |
| --- | --- | --- | --- |
| 1 | (도입) | "배송 조회가 자주 실패한다"는 문의 | 배송 서비스는 모니터링이 없는 Python 서비스. 코드는 바뀐 게 없다 |
| 2 | 서비스 목록 | 회원·상품·주문·결제·배송 다섯 서비스가 언어 아이콘(Java·Go·Node.js·Python)과 함께 나타나 있음 | 아무도 계측하지 않았는데 이미 목록에 있다 |
| 3 | 토폴로지 맵 | 배송 서비스 노드의 경고, 외부 택배사 API 로 나가는 연결선의 **연결 실패** | 배송 서비스에서 밖으로 나가는 연결에 문제가 있다 |
| 4 | 배송 서비스 상세 → SLO 탭 | 주문 서비스에서 들어온 요청이 약 5초 뒤 오류로 끝남. Client 표에서 프로토콜이 HTTP 로 자동 구분 | 5초 뒤에 실패한다. 프로토콜은 누가 알려주지 않아도 HTTP 로 구분됐다 |

## 영상 ② 원인 (12분) — 데모 2, 3

| 순서 | 화면 | 보여줄 것 | 말할 것 |
| --- | --- | --- | --- |
| 1 | APM 대시보드 → T-Map | 5초 부근 요청 영역을 드래그 → **트랜잭션 조회** 창에서 배송 서비스 요청이 전부 약 5초 + 5xx | 느린 요청은 전부 배송 서비스, 전부 5초, 전부 5xx |
| 2 | 배송 서비스 상세 → 도메인네임(DNS) 탭 | 택배사 도메인(`api.courier.example`) 조회 정상, NXDOMAIN·ServFail 0 | 이름 조회는 멀쩡하다 |
| 3 | 배송 서비스 상세 → 네트워크 탭 | "Failed TCP connections" 차트, 실패한 연결 목적지가 **새 IP:443** | 연결이 실패하는 곳은 택배사의 새 IP 주소다 |
| 4 | (조회 기간 확대) | 예전에는 예전 IP 로 정상 연결되던 모습 | 어제까지는 다른 IP 로 잘 붙었다 |
| 5 | 터미널: `./demo.sh firewall` | 방화벽 규칙에 예전 IP 만 있고 새 IP 는 없음 | DNS 는 정상인데 새 IP 로의 연결만 실패 → 경로상 차단을 의심 → 방화벽에 새 IP 가 없다 |

`./demo.sh firewall` 출력 예:

```
RULE                            DESCRIPTION
fw-allow-courier-10-0-0-61      택배사 API (api.courier.example) 10.0.0.61:443 허용
fw-delivery-default             배송 서비스 egress 기본 규칙: DNS 만 허용, 그 외 차단
```

## 영상 ③ 해결과 표준화 (10분) — 데모 4

```bash
./demo.sh fix             # 방화벽에 새 IP:443 허용 추가
./demo.sh firewall        # 새 규칙이 추가된 것 확인
./demo.sh status          # TCP 연결 성공, 배송 조회 HTTP 200
```

| 순서 | 화면 | 보여줄 것 | 말할 것 |
| --- | --- | --- | --- |
| 1 | 배송 서비스 상세 → 네트워크 탭 | 방화벽 허용 이후 실패한 연결이 0 | 고친 뒤 실패한 연결이 없어졌다 |
| 2 | APM 대시보드 → T-Map | 5초 영역이 사라짐 | 사용자 요청도 정상으로 돌아왔다 |
| 3 | 서비스 목록 (또는 커스텀 대시보드 RED 템플릿) | Java·Go·Python·Node.js 다섯 서비스를 요청 수·지연·오류로 나란히 비교 | 언어·팀·도구가 달라도 같은 기준으로 본다 |
| 4 | 한계 장표 | 서비스 간 요청 1건 추적, 앱 내부 외부 호출 실패 원인 → OpenTelemetry 와 함께 (EP05) | eBPF 가 못 보는 부분은 다음 편에서 |

- `./demo.sh fix` 직후 바로 녹화하지 말고 1~2분 기다려 차트에 변화가 반영된 뒤 보여줍니다.

## 다음 테이크 준비

```bash
./demo.sh reset           # 방화벽 새 IP 규칙 삭제, DNS → 예전 IP
# 정상 데이터가 몇 분 쌓인 뒤
./demo.sh incident
```

---

## 촬영 전 확인 (실제 화면에서)

- [ ] 연결에 한 번도 성공하지 못한 외부 목적지(새 IP)가 토폴로지 맵에 노드로 그려지는가
- [ ] 연결 실패만 있는 연결선이 토폴로지 맵에서 빨간색(CRITICAL)으로 표시되는가 — 연결선 색은 L7 오류율 기준이라 표시되지 않을 수 있음
- [ ] 배송 서비스 노드 경고: 주문 → 배송 요청의 오류율이 SLO 임계치(기본 가용성 99%)를 넘는가
  - 장애 중 배송 조회는 100% 실패하므로 넘어야 정상. 안 보이면 SLO 평가 주기만큼 더 기다린다
- [ ] 네트워크 탭의 "실패한 연결 목적지" 표시(OBSERV-684, 2026-09-09 구현)가 촬영 버전에 들어 있는가
- [ ] 드래그로 열리는 창 이름이 "트랜잭션 조회"인가 ("요청 뷰어"는 다른 위젯 — 대본에서 섞어 쓰지 않음)
- [ ] 스팬 워터폴은 EP05 범위 — 이 편에서는 트랜잭션 조회 목록까지만
- [ ] Cassandra·RabbitMQ 는 이 편에서 언급하지 않음 (이 데모에는 없음)
- [ ] 서비스 목록의 언어 아이콘: 회원·주문 Java / 상품 Go / 결제 Node.js / 배송 Python
- [ ] DNS 탭: `api.courier.example` 조회가 보이고 NXDOMAIN·ServFail 이 0

## 대본에서 쓰지 않는 표현

| 쓰지 않는 표현 | 이유 | 대신 쓰는 표현 |
| --- | --- | --- |
| "택배사 API 호출이 5초 타임아웃" | 응답 전에 끊긴 외부 호출은 eBPF 기록이 남지 않음 | "배송 서비스 요청이 5초 뒤 실패" |
| "외부 API 호출의 오류율·지연이 높다" | 연결 자체가 막혀 HTTP 요청이 없으므로 오류율·지연 값이 없음 | "연결 실패" |
| "DNS 가 새 IP 를 돌려받고 있다 (DNS 화면에서)" | DNS 응답 IP 를 보여주는 화면이 없음 | 새 IP 는 네트워크 탭의 실패한 목적지로 보여줌 |
| "토폴로지에서 HTTP·DNS·MySQL 이 구분되어 보인다" | 토폴로지 연결선에는 프로토콜 표시가 없음 | SLO 탭 Client 표, DNS 탭, MySQL 탭에서 보여줌 |
